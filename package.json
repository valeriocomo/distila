{
  "name": "distila",
  "version": "1.1.0",
  "private": true,
  "description": "Distila: on-device article summarizer for Chromium browsers.",
  "scripts": {
    "sync-version": "node scripts/sync-version.js",
    "build": "node scripts/build-zip.js",
    "version": "npm run sync-version && git add src/manifest.json",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "archiver": "^7.0.1",
    "husky": "^9.1.7"
  }
}
